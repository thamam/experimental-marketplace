# Claude Memory MCP Server Configuration

# Memory storage directory (default: ~/.claude-memory)
MEMORY_DIR=~/.claude-memory

# Embedding model to use (options: minilm-l6, minilm-l12, mpnet-base)
# - minilm-l6: Fast, small (80MB), good quality [RECOMMENDED]
# - minilm-l12: Slower, larger (120MB), better quality
# - mpnet-base: Highest quality (420MB), best accuracy
EMBEDDING_MODEL=minilm-l6

# Maximum memory storage size in MB (default: 1000)
MAX_MEMORY_SIZE_MB=1000

# Auto-archive memories older than N days (default: 90)
AUTO_ARCHIVE_DAYS=90

# Enable automatic indexing of memories (default: true)
ENABLE_AUTO_INDEXING=true
